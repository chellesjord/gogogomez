import React from 'react';
import PhotoList from '../components/PhotoList';

const GalleryPage = () => {
    return (
        <main>
            <section>
                <PhotoList />
            </section>
        </main>
    );
};

export default GalleryPage